import React from 'react';
import logo from '../imgs/logo.png'
import '../CSS/logo.css'

const Logo = () => {
return (
    <div className='mlogo'>            
        <img src={logo} alt='' />
    </div>
)
};

export default Logo;